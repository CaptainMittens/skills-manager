# RPG Session Handoff — skills-manager

Generated: 2026-05-16 ~20:39 UTC
Trigger: Planning session complete; context closing before execution begins.

## What Was Done This Session

### NASA Software-Engineering Audit

Ran a full structural audit of the skills-manager codebase against NASA Power-of-Ten rules. `npx tsc --noEmit` and `cargo check` both returned zero errors/warnings. Rule 4 (small functions/files) and cyclomatic complexity failed. Top-5 audit findings, ranked:

1. Monolith view files: `ProjectDetail.tsx` (2428 lines), `MySkills.tsx` (1922), `InstallSkills.tsx` (1934), `Settings.tsx` (1741) — ProjectDetail.tsx is the primary target.
2. God-object `AppContext.tsx` (11 state slices + 10 callbacks).
3. 16× non-null `!` assertions on optional callbacks in `SkillCard.tsx`.
4. Production `.unwrap()` / `.expect()` panics: `tool_adapters.rs:46`, `lib.rs:369`, `central_repo.rs:38`.
5. O(n²) LCS with no input guard in `DocumentDiffViewer.tsx:47`.

User elected to tackle finding #1 first.

### Grep-Verified Guardrail Corrections

Verified via grep (exit 1 for all) that `ProjectDetail.tsx` contains:

- **Zero @dnd-kit code** — `DndContext`, `SortableContext`, `collisionDetection`, `DragOverlay`, `useSortable`, `onDragStart`, `onDragEnd` are all absent. The documented dnd guardrail (byte-unchanged since `130234e`) is "do not introduce", not "preserve verbatim".
- **Zero `<SkillCard>` renders** — bespoke inline grid/list card markup exists at lines ~1194–1563. The SkillCard `React.memo` areEqual contract is a pattern to honor (keep `useCallback` identities stable), not a binding boundary here.
- **`createPortal` IS used** — imported line 24, used line 2425, but only for the `AddFromLibraryDialog` modal. Must be relocated byte-identical.
- **AppContext coupling is narrow** — only `projects`, `scenarios`, `managedSkills`, `refreshManagedSkills`, `refreshScenarios`, `refreshProjects` (lines ~176–183). God-object refactor is OUT OF SCOPE for this quest.

### User Decision Recorded

Cohesion-over-line-count rule saved to memory at `~/.claude/projects/-Users-joshuarichter-Documents-Projects-skills-manager/memory/feedback-file-cohesion.md`:

- ≤300-line target is FLEXIBLE; cohesion wins over arbitrary splitting.
- Three modules exceeding 300 lines after mechanical split are accepted as final shape: `useProjectSkillMutations.ts` (~450), `AddFromLibraryDialog.tsx` (~625), `ProjectSkillGrid.tsx` (~370) — each is a single complete responsibility. No second sub-split pass needed.

### 6-Stage Decomposition Plan Written

(See "What's NOT Done" for the full plan to execute.)

---

## Active State

- **Branch:** `main` at `6a1a65a`
- **Working tree:** clean
- **All commits pushed:** yes — branch is up to date with `origin/main`
- **No code changed this session** — planning and audit only; everything is reversible
- **DM state:** Hale
- **Familiar:** none summoned (planning artifact, no unresolved verdict)
- **Mirrors:** not applicable (no skills/ or agents/ edits this session)

---

## What's NOT Done

The entire 6-stage decomposition of `src/views/ProjectDetail.tsx` is unexecuted. Plan is complete and ready for `subagent-driven-development-rpg`.

### Stage 0 — Baseline gate (no code change)

- Run `npx tsc --noEmit` (NOT `tsc -b`) and `npx vitest run` to capture green oracle.
- Commit message: `test(baseline): capture green tsc + vitest oracle before ProjectDetail decomp`

### Stage 1 — Extract pure helpers + shared type (~-115 lines)

- Move from `ProjectDetail.tsx`:
  - Lines ~51–58: constants
  - Lines ~60–74: `ProjectSkillGroup` interface
  - Lines ~76–170: `getDefaultExportAgents`, `getSyncStatusMeta`, `getAssignedAgents`, `getAgentDotTargets`, `getGroupStatus`
- Destination: `src/views/projectDetail/projectSkillUtils.ts` (new file)
- Gate: `npx tsc --noEmit` + `npx vitest run` green; then commit.

### Stage 2 — Extract AddFromLibraryDialog (~-622 lines)

- Move lines ~1807–2428 verbatim, including `createPortal` (line 2425) and its internal `useMultiSelect`.
- Destination: `src/views/projectDetail/AddFromLibraryDialog.tsx` (new file)
- Export existing prop interface (lines ~1817–1827) unchanged.
- Gate: `npx tsc --noEmit` + `npx vitest run` green; then commit.

### Stage 3 — Extract ProjectSkillDetailPanel (~-174 lines)

- Move lines ~1632–1805 verbatim.
- Destination: `src/views/projectDetail/ProjectSkillDetailPanel.tsx` (new file)
- Export prop interface (lines ~1643–1651) unchanged.
- Gate: `npx tsc --noEmit` + `npx vitest run` green; then commit.

### Stage 4 — Extract useProjectSkills data hook (~-180 lines)

- Move logic ~lines 237–489: `loadSkills`, agent-targets effect, redirect effect, `groupedSkills` memo, detail-refresh effect, `exportTargets`, `projectSkillDirNamesByAgent`, `projectCenterSkillIdsByAgent`, `projectPresetVariants`/`findProjectPresetVariant`, default-export effect, `detailSkill`+`doc-content` states.
- Destination: `src/views/projectDetail/useProjectSkills.ts` (new file)
- Keep returned callbacks `useCallback`-wrapped for stable identities.
- **RECOMMEND:** Add a `ProjectDetail` render smoke test BEFORE this stage — tsc+vitest alone won't catch behavioral regressions from effect-boundary moves.
- Gate: `npx tsc --noEmit` + `npx vitest run` green; then commit.

### Stage 5 — Extract useProjectSkillMutations hook (~-440 lines)

- Move lines ~490–933: all single + batch + preset handlers.
- Destination: `src/views/projectDetail/useProjectSkillMutations.ts` (new file, ~450 lines — accepted per cohesion rule)
- Mechanical move only — NO logic rewrite. Preserve `Promise.all` ordering and `await` sequencing EXACTLY.
- Gate: `npx tsc --noEmit` + `npx vitest run` green; then commit.

### Stage 6 — (Optional) Extract ProjectSkillGrid presentational subtree (~-370 lines)

- Move card render block ~lines 1194–1563 (grid + list).
- Destination: `src/views/projectDetail/ProjectSkillGrid.tsx` (new file, ~370 lines — accepted per cohesion rule)
- Preserve `key={skillKey}` / `key={skill.id}` verbatim.
- Do only if main file still too large OR explicitly desired — lowest architectural gain of the six stages.
- Gate: `npx tsc --noEmit` + `npx vitest run` green; then commit.

---

## Projected Final Tree

```
src/views/ProjectDetail.tsx                          (~210-280, orchestration only)
src/views/projectDetail/projectSkillUtils.ts         (~120)
src/views/projectDetail/useProjectSkills.ts          (~190)
src/views/projectDetail/useProjectSkillMutations.ts  (~450 — accepted)
src/views/projectDetail/ProjectSkillDetailPanel.tsx  (~175)
src/views/projectDetail/AddFromLibraryDialog.tsx     (~625 — accepted)
src/views/projectDetail/ProjectSkillGrid.tsx         (~370 — accepted, optional)
```

---

## DO-NOT-DO List (enforced at every stage)

- Do NOT introduce any `@dnd-kit` import / `DndContext` / `SortableContext` / `DragOverlay` / `useSortable` into `ProjectDetail.tsx` or any new module.
- Do NOT rewrite the `createPortal` modal mount (line 2425) — relocate byte-identical.
- Do NOT change `key={skillKey}` / `key={skill.id}` expressions when relocating JSX.
- Do NOT convert the bespoke inline cards (~1220–1561) to `<SkillCard>` — that is a behavior change, out of scope.
- Do NOT pass fresh inline arrow functions as props to memoized children — reuse / keep `useCallback`-wrapped handlers.
- Do NOT touch `AppContext.tsx` — god-object refactor is out of scope.
- Do NOT reorder `Promise.all([loadSkills(), refreshProjects()])` / `refreshManagedSkills()` sequences.
- Do NOT merge stages — each must pass its own independent tsc+vitest gate for bisectable regressions.

---

## Risk Register

| Risk                                        | Mitigation                                                                                                |
| ------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| No existing `ProjectDetail` test            | Add smoke test BEFORE Stages 4–5; non-blocking for Stages 1–3                                             |
| dnd guardrail                               | Verified zero dnd code in file; rule = "never introduce"; relocate `createPortal` + `key=` byte-identical |
| SkillCard memo contract                     | Not used in ProjectDetail; honor by keeping `useCallback` identities stable                               |
| AppContext coupling                         | Narrow and stable; consume existing surface only; out of scope                                            |
| Behavioral drift from effect boundary moves | Per-stage move-verbatim + tsc+vitest gate; preserve async ordering                                        |

---

## Commits This Session

| Hash      | Description                                                                                   |
| --------- | --------------------------------------------------------------------------------------------- |
| `6a1a65a` | docs(handoffs): record SkillCard extraction + perf-fix continuation                           |
| `99e8447` | perf: memoize SkillCard to stop ~119-card re-render storm (fixes post-refactor slowdown)      |
| `f874a40` | refactor: use SkillCard for grid-view card; tighten tagInputRef typing                        |
| `c9461d1` | refactor: use SkillCard for list-view card                                                    |
| `b925a58` | refactor: use SkillCard for both DragOverlay clones (fixes grid-overlay missing-footer drift) |

_(No new commits this session — planning only.)_

---

## How to Resume

1. Read this handoff in full.
2. Re-verify repo is still clean and green:
   ```bash
   git status
   npx tsc --noEmit
   npx vitest run
   ```
3. Execute the plan using `subagent-driven-development-rpg`: dispatch a fresh agent per stage, Stage 0 first, tsc+vitest gate + commit after each stage, review checkpoint between stages.
4. Critical files for the implementer to read before starting:
   - `src/views/ProjectDetail.tsx` — the 2428-line monolith being decomposed
   - `src/context/AppContext.tsx` — AppContext surface (consume only; do not modify)
   - `src/components/SkillCard.tsx` — memo areEqual contract to honor
   - `src/hooks/useMultiSelect.ts` — used inside AddFromLibraryDialog; may need re-export
   - `src/lib/tauri.ts` — tauri bridge; referenced by mutation handlers
5. Stages 1–3 are the safe ~900-line leaf extractions; do these first.
6. Add a `ProjectDetail` render smoke test before dispatching Stages 4–5 (logic hooks).
7. Stage 6 (ProjectSkillGrid) is optional — revisit after Stage 5 lands.
